PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Optional — only needed if you switch from the mock JSON data
# to a real MongoDB database in server/data
MONGO_URI=mongodb://localhost:27017/smart-home-fashion-hub